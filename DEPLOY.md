# Deploy a Hostinger

1. Ejecutar: `npm run build`
2. Subir TODO el contenido de la carpeta `dist/` al `public_html/` de Hostinger (via File Manager o FTP)
3. Verificar que `.htaccess` esté en la raíz de `public_html/`
4. Listo — la web queda online en el dominio configurado
